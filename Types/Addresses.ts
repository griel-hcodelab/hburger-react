export type TypeAddresses = {
    id?: number,
    street: string,
    number?: string,
    complement?: string,
    district: string,
    city: string,
    state: string,
    zipcode: string | number,
}